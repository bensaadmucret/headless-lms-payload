# Configuration pour les tests
NODE_ENV=test

# Base de données de test (utilise le même conteneur Docker que le dev)
PAYLOAD_TEST_DATABASE_URI=postgres://payload:payload123@localhost:5433/medcoach_db

# Configuration Payload pour les tests
PAYLOAD_SECRET=test-secret-key-for-ci-cd
PAYLOAD_CONFIG_PATH=src/payload.config.ts
PAYLOAD_PUBLIC_SERVER_URL=http://localhost:3000

# Désactiver les logs verbeux pendant les tests
PAYLOAD_DISABLE_ADMIN=true